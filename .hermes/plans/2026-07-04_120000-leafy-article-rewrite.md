# Leafy 中文文章精简重写计划

> **For Hermes:** 直接执行，不需要 subagent-driven-development。

**Goal:** 精简 `zh/projects/leafy.md`，删除无关噪音（图标、过于细节的 UI 参数、啰嗦的功能罗列），突出项目组织方式和核心特色。

**Architecture:** 单文件重写。砍掉废话、压缩啰嗦描述、保留技术亮点。预期从 181 行压缩到约 120-130 行。

**Tech Stack:** Markdown（VitePress 博客文章）

---

## 问题诊断

用户反馈的核心问题：

1. **图标是噪音。** Tab 图标（`sparkles`、`calendar`、`person.2` 等）对理解项目没有任何价值，只是 SF Symbols 名字，读者不需要知道
2. **功能罗列太啰嗦。** 尤其是"学业"Tab 的 8 类工具逐个展开，以及"课表页具体功能"一长串列表——这些细节对理解项目特色帮助不大
3. **设计系统参数过于细化。** 圆角三档、间距五档、字体修饰名（`responsiveLargeTitle()` 等）——这些都是实现细节，读者不需要逐条知道
4. **部分段落废话偏多。** 比如"我的"Tab、"浮动按钮"描述等

### 分类判断标准

| 类别 | 判断 | 处理 |
|------|------|------|
| **图标**（`sparkles`、`calendar`、`person.2`、`book.closed`、`person`） | 纯噪音 | **删** |
| **浮动按钮逐 Tab 描述**（L48） | 过度细化 | **压缩**成一句 |
| **课表具体功能逐条罗列**（L71） | 信息密度低 | **压缩**成概述 |
| **学业 8 类工具逐个展开**（L97-L106） | 过度细化 | **归类概述** |
| **"我的"Tab 描述**（L110） | 啰嗦 | **压缩**成一段 |
| **设计系统参数**（L118-L120：密度档位/字体修饰/圆角/间距） | 实现细节 | **删**掉具体参数，保留核心思想 |
| **设计系统根导航**（L124） | 有价值 | 保留 |
| **技术架构表**（L128-L137） | 有价值 | 保留 |
| **工程分层目录**（L141-L151） | 一般 | 精简或保留 |
| **工程收获**（L175-L181） | 核心亮点 | **保留并强化** |
| **Tab 承载内容描述**（L42-L46） | 必要 | 保留但去掉图标列 |
| **数据源边界**（L50-L57） | 有价值 | 保留 |
| **五层回退**（L61-L69） | 核心特色 | 保留 |
| **Leafy AI**（L77-L83） | 有价值 | 保留，去掉图标 |
| **社区**（L85-L93） | 有价值 | 保留 |
| **运营后台**（L155-L159） | 有价值 | 保留 |
| **App Store 上架**（L161-L165） | 有价值 | 保留 |
| **未来规划**（L167-L173） | 有价值 | 保留 |

---

## 修改步骤

### 步骤 1：修改"功能全景"部分（L38-L48）

**改动：**
- 删掉 Tab 表格中的"图标"列
- 把浮动按钮描述从逐 Tab 罗列压缩成一句

**修改后：**

```
App 以五个根 Tab 组织：

| Tab | 承载内容 |
|---|---|
| **Leafy** | AI 助手，围绕课表、考试、成绩提问；支持自备 DeepSeek API Key 或 Leafy 托管模式 |
| **课表** | 周视图课表网格、课程详情、考试/提醒/重要日期投射、课表底图、时间视角 |
| **社区** | 帖子流、发布、搜索、投票、通知、公告、举报、屏蔽 |
| **学业** | 成绩、自习室、学习、体育、教学培养、职业规划、考研信息、评教/评课/评菜 |
| **我的** | 社区资料、个人内容、共享课表、主题色、深色模式、反馈、退出 |

每个 Tab 右侧有浮动动作按钮，统一承载各页面的快捷操作入口。
```

### 步骤 2：压缩"课表"部分（L71）

**改动：** 把逐条功能罗列压缩为概述

**修改前（L71-L73）：**
```
课表页具体功能：全学期周切换、自动识别当前周、隐藏周末、手动刷新学校课表、课程详情 sheet（课程名/教师/教室/周次/节次）、同节次重叠课程并排压缩、课程备注、空白节次添加本地提醒、考试/重要日期投射到课表格子、日期头点击查看当天摘要、iPad agenda 列表模式、自定义课表底图（图片/透明度/模糊/遮罩/课程卡片透明度）。

快捷入口：共享课表、成绩、找自习室、课表底图。
```

**修改后：**
```
课表页覆盖全学期周切换、课程详情、重叠课程并排压缩、课程备注、本地提醒、考试倒计时投射、iPad 列表模式、自定义课表底图等功能。
```

### 步骤 3：压缩"学业"部分（L97-L106）

**改动：** 把 8 类工具逐个展开压缩为归类概述

**修改前：**
```
学业页通过一级功能切换组织 8 类校园工具：

- **成绩**：查询课程成绩、绩点、各学期记录；成绩分析和趋势展示；考试安排；荣誉记录（导入和管理 PDF/图片）
- **自习室**：按楼宇/日期/起止节次查询空教室；指定教室占用查询；常用自习室收藏；图书馆座位预约外部链接；专注记录
- ...（共 8 条）
```

**修改后：**
```
学业页通过一级功能切换组织校园工具：成绩查询与趋势分析、空教室与自习室、学习项目管理、体育记录（阳光长跑/体测/场馆）、教学培养方案、职业规划与简历管理、考研信息聚合、评教评课评菜。
```

### 步骤 4：精简"我的"部分（L108-L110）

**改动：** 去掉啰嗦的 UI 组件描述

**修改前：**
```
系统 `List + insetGrouped`。资料区展示社区头像、昵称、学号绑定说明和学院/年级。内容区包含我的发帖、点赞、评论、收藏、投票。功能区包含共享课表、个性化设置、课表底图、中英文切换、隐藏周末。支持区包含使用手册、反馈提交、缓存同步说明、App Store 评分、常用链接和联系我们。退出登录使用红色危险操作，二次确认后清理学校会话和社区会话。
```

**修改后：**
```
"我的"页包含社区资料、我的内容（发帖/点赞/评论/收藏/投票）、共享课表、个性化设置（主题色/深色模式/中英文/隐藏周末）、使用手册、反馈、App Store 评分和退出登录。
```

### 步骤 5：精简"设计系统"部分（L112-L124）

**改动：** 
- 删掉字体修饰名（L118 后半段 `responsiveLargeTitle()` 等）
- 删掉圆角三档/间距五档（L120）
- 保留主题色、密度控制核心思想、玻璃材质、根导航

**修改后：**
```
## 设计系统

主题、颜色、间距、字体集中定义在 `AppTheme.swift`。

主题色 4 种：鼠尾草绿（默认）、蒂芙尼蓝、糖果莓粉、自定义颜色。语义色包括 `accent`、`danger`、`warning` 等。显示密度 4 档，通过 `leafyFontScale` 和 `leafyControlScale` 统一控制。

玻璃和材质通过 `LeafyPageBackground()`、`.leafyCardStyle()`、`.leafyGlassSurface(...)` 封装：iOS 26 使用系统 `glassEffect`，低版本回退到 `topBarMaterial` 和描边。图标优先使用 SF Symbols，英文品牌标题使用 `Lora` 字体。

根导航在 iOS 26 使用系统 `TabView` 和 tab bar；iOS 17-25 使用自定义底部 `RootFloatingTabBar`——一条玻璃胶囊 Tab 区加右侧独立圆形浮动动作按钮。窄屏减少横向边距，极窄宽度下隐藏 Tab 文案只保留图标。
```

### 步骤 6：去掉 Leafy AI 标题下的图标引用（L79）

**改动：** 删掉 "用 `sparkles` 图标" 这句废话

**修改前：**
```
Leafy AI 是独立根 Tab，用 `sparkles` 图标。围绕课表、考试、成绩、培养方案和本机学习记录提问。
```

**修改后：**
```
Leafy AI 是独立根 Tab，围绕课表、考试、成绩、培养方案和本机学习记录提问。
```

### 步骤 7：验证

1. 运行 `npm run dev` 确认文章渲染正常
2. 通读全文检查流畅度

---

## 不改动的部分

以下部分保持原样：
- 开头 + 链接表
- 截图展示
- 数据源边界表
- 课表五层回退描述
- 社区部分
- 技术架构表 + 工程分层
- 运营后台
- App Store 上架
- 未来规划
- 工程收获

---

## 预期效果

- 行数从 181 行压缩到约 120-130 行
- 去掉所有 SF Symbols 图标引用
- 去掉圆角/间距/字体修饰名等实现细节
- 压缩啰嗦的功能罗列为概述
- 核心特色（五层回退、预投影、完整产品链路）保留并更突出
